# This static portfolio does not require environment variables by default.
# Add future deploy-time settings here without committing real secrets.

# Example:
# CONTACT_FORM_ENDPOINT=https://example.com/api/contact
# ANALYTICS_ID=replace-with-public-analytics-id
